# Code Improvement Queue

Append-only proposals for unrelated production and test code improvements. Follow the
[improvement proposal protocol](./IMPROVEMENTS_PROTOCOL.md).

**Entry format:**

```
- [YYYY-MM-DD] target: src/path/file.ts[:symbol] · effort: <0-100>/100 · type: fix|better|refactor · observation: … · suggestion: … · source-rule: docs/rules/<name>.md (optional) · [model]
```

## Entries

<!-- newest first; remove a line only when a focused improvement task handles it -->

- [2026-09-19] target: src/utils/**/service/*.service.ts telemetry wrappers · effort: 45/100 · type: refactor · observation: ~25 service files each define near-identical `logServerEvent(level, event, { requestId, path: serverFn:<action>, status, durationMs, actorId, ...fields })` wrappers (Fallow duplication warnings in `course.service.ts` `logCourseReadEvent`/`logCourseMutationEvent`, 7 copies in `enrolment.service.ts`, plus exam/assignment/event/post/library/etc.). Warn-only — gate passes — but the pattern is pasted per file with only the context type differing. · suggestion: extract a shared `logServiceEvent(level, event, context: { action, actorId, ...entityIds, startedAt }, fields)` helper (e.g. `src/utils/observability/service-event.ts`) and collapse the per-service wrappers in a dedicated pass; keep per-service event names/context types intact. · source-rule: docs/rules/complexity.md · [model]
- [2026-09-17] target: src/utils/request-scope.test.ts · effort: 40/100 · type: fix · observation: `withRequestScope > caches repeated role checks` fails in the full `vitest run` (`isRole` returns false — the file's `vi.mock('@/db')` factory loses to a leaked mock) but passes alone and pairwise against `authz/index`, `authz/cache`, `authz/default-adapter`, and `notifications/delivery` tests. Reproducible, order-dependent — `vitest.config.ts` sets `isolate: false`, so a `vi.mock('@/db')` (or authz internals) from another file persists across the shared module graph. Pre-existing on the branch; unrelated to any working-tree diff (the unit glob excludes `*.integration.test.ts`). · suggestion: bisect the full suite to find the leaking file and `vi.unmock`/restore its `@/db` mock, or give `request-scope.test.ts` its own registry (`isolate: true` per-file via `// @vitest-environment` pragma or a project split). · source-rule: docs/rules/complexity.md · [model]
- [2026-09-17] target: src/components/library/media-card/media-card.domain.ts:MediaCardSize · effort: 10/100 · type: fix · observation: the `'mobile'` size (`w-full` in `WIDTH_CLASSES`) is covered by `media-card.domain.test.ts` but never passed from any JSX — dead code. · suggestion: remove `'mobile'` from the union, its `WIDTH_CLASSES` entry, and the test case; or wire it where a full-width card is actually intended. · [model]
- [2026-09-17] target: src/components/library/LibraryShelf.tsx:MediaCardWithActions · effort: 20/100 · type: better · observation: manage actions render `hidden group-hover:flex`; touch devices have no hover, so edit/delete is unreachable on mobile and a first tap may only reveal actions instead of navigating (same pattern in `CourseDetailSections.tsx:MaterialCardItem`). · suggestion: also reveal on `group-focus-within`, or show a persistent affordance on coarse pointers (`[@media(hover:none)]:flex`). · [model]
- [2026-09-17] target: src/utils/courses/courses.integration.test.ts · effort: 30/100 · type: refactor · observation: Fallow reports 10 duplicated-block warnings: a 7-line block ×9 (lines ~1043, ~1188), an 8-line block ×5 (lines ~1095, ~1148, ~1187), a 14-line block ×3 (line ~661), a 10-line block ×3 (lines ~1235, ~1269, ~1303), and a 7-line block ×4 (line ~147). Warn-only: test files are health-ignored by design (ADR 0011) and `quality:gate` passes. Unrelated to the vitest `fsModuleCache` change. · suggestion: extract colocated helpers in a dedicated test-cleanup pass, same pattern as the enrolment.integration.test.ts entry; keep assertions inline where sharing hurts readability. · source-rule: docs/rules/complexity.md · [model]
- [2026-09-17] target: src/utils/assignments/repository/submissions.repository.ts:updateSubmissionGrade · effort: 20/100 · type: fix · observation: `gradeSubmissionService` writes `grade`/`gradedAt` but never `status = 'graded'` — submissions stay `status='submitted'` after grading. `buildAssignmentStats` (course.domain.ts) and `assignments-view.domain.ts` count `status === 'graded'`, so graded counts/chips undercount everywhere; the codebase otherwise derives gradedness from `grade !== null` (`resolveSubmissionStatusVariant`). Found while deriving lesson completion (ADR 0024). · suggestion: either write `status='graded'` in `updateSubmissionGrade`, or make all read sites derive from `grade IS NOT NULL` consistently; add a regression test asserting the chosen invariant. · [model]
- [2026-09-17] target: src/utils/whatsapp/whatsapp.integration.test.ts · effort: 30/100 · type: refactor · observation: Fallow reports 10 duplicated-block warnings: a 7-line block ×14 and 5-line block ×14 (lines ~326–487), an 8-line block ×8 (line ~615), and several ×3 groups (lines ~657–759). Warn-only: test files are health-ignored by design (ADR 0011) and `quality:gate` passes. Unrelated to the vitest `isolate: false` change. · suggestion: extract colocated helpers in a dedicated test-cleanup pass, same pattern as the enrolment.integration.test.ts entry; keep assertions inline where sharing hurts readability. · source-rule: docs/rules/complexity.md · [model]
- [2026-09-17] target: scripts/quality-fallow.mjs · effort: 20/100 · type: fix · observation: `fallow audit` auto-detects a local `coverage/coverage-final.json` for CRAP scoring, but the gate never generates or passes `--coverage`; a stale dev coverage file masks `introduced` CRAP findings locally (verdict warn) while a fresh CI checkout has none (verdict fail) — quality:gate result depends on local machine state. · suggestion: make the gate deterministic — either run `vitest run --coverage` before the fallow lane and pass `--coverage coverage/coverage-final.json` (with `--coverage-root`), or explicitly disable coverage-based scoring so local and CI agree. · source-rule: docs/rules/complexity.md · [model]
- [2026-09-17] target: src/utils/enrolment/enrolment.integration.test.ts · effort: 30/100 · type: refactor · observation: Fallow reports 10 duplicated-block warnings: a 6-line spy→JSON event-parse idiom ×13, a 15-line `withObservabilityRequest` param-table loop ×3, and 10-line `expect.objectContaining` event assertions ×4–5. Warn-only: test files are health-ignored by design (ADR 0011) and `quality:gate` passes with 49 clone groups. · suggestion: extract colocated helpers (e.g. `findLoggedEvent(spy, name)`, `runWithRequestId(action, run)`, a failed-event matcher builder) in a dedicated test-cleanup pass; keep assertions inline where sharing hurts readability. · source-rule: docs/rules/complexity.md · [model]
- [2026-08-17] target: src/utils/assignments/service/assignments.service.ts · effort: 20/100 · type: refactor · observation: Fallow flags a 12-line duplicated block at lines 225, 495, and 517. Unrelated to Staff Privileges. · suggestion: extract the shared block once in a later assignments-only change. · source-rule: docs/rules/complexity.md · [model]
- [2026-08-17] target: src/routes/_authed/enrollments/index.tsx:EnrollmentsPage · effort: 20/100 · type: refactor · observation: Fallow CRAP finding persists on the page body (pre-existing). Extracting dialogs into a new function introduced a blocking CRAP finding because the extract had no coverage. · suggestion: add a page-level render test or finish decomposition under tests so future header/dialog work does not trip introduced-complexity. · source-rule: docs/rules/complexity.md · [model]
- [2026-07-25] target: scripts/seed-development.ts:findUserByEmail · effort: 15/100 · type: fix · observation: `bun run db:seed:development` missed an existing auth user, attempted `createUser`, and failed with Supabase `email_exists`, leaving canonical development credentials stale. · suggestion: make user pagination/lookup reliably find existing emails and cover the duplicate-user reconciliation path with a focused test. · [model]
- [2026-07-20] target: src/components/course/CourseDetailSections.tsx · effort: 5/100 · type: fix · observation: `tsc` reports unused `role` param (TS6133). · suggestion: remove unused binding or prefix `_role`. · [model]
- [2026-07-20] target: src/components/ui/pagination.tsx · effort: 5/100 · type: fix · observation: `tsc` reports unused `PaginationLink` (TS6133). · suggestion: export/use or remove dead import/local. · [model]
- [2026-07-20] target: src/utils/student/service/student.service.ts:getStudentDetailService · effort: 25/100 · type: better · observation: detail "enrollments" still mirrors all courses with hardcoded `active` status (called out in integration test). · suggestion: wire real course enrollments when product needs it. · [model]
